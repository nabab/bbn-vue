((bbn)=>{let script=document.createElement('script');script.innerHTML=`<div :class="[
        'bbn-textbox',
        componentClass,
        {'bbn-state-disabled': !!isDisabled}
      ]">
  <textarea :class="{
              'bbn-no-border': true,
              'bbn-radius': true,
              'bbn-w-100': !cols,
              'bbn-spadded': true,
              'bbn-state-disabled': !!isDisabled
            }"
            :value="value"
            :name="name"
            ref="element"
            @input="emitInput($event.target.value)"
            @click="click($event)"
            @focus="focus($event)"
            @blur="blur($event)"
            @change="change($event)"
            @keydown="textareaKeydown($event)"
            @keydown.enter.stop
            @keyup="keyup($event)"
            @paste="$emit('paste', $event)"
            :disabled="isDisabled"
            :readonly="readonly"
            :required="required"
            :placeholder="placeholder"
            :maxlength="maxlength"
            :rows="rows"
            :cols="cols"
            :style="{resize: !resizable ? 'none' : ''}"/>
</div>
`;script.setAttribute('id','bbn-tpl-component-textarea');script.setAttribute('type','text/x-template');document.body.insertAdjacentElement('beforeend',script);(function(bbn){"use strict";Vue.component('bbn-textarea',{mixins:[bbn.vue.basicComponent,bbn.vue.inputComponent,bbn.vue.eventsComponent],props:{rows:{type:Number},cols:{type:Number},maxlength:{type:Number},resizable:{type:Boolean,default:true}},methods:{textareaKeydown(ev){if(this.maxlength&&(this.value.length>=this.maxlength)){ev.preventDefault();}
else{this.keydown(ev);}},clear:function(){this.emitInput('');}},mounted(){this.ready=true;}});})(bbn);})(bbn);