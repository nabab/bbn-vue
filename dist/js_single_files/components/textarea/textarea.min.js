((bbn)=>{let script=document.createElement('script');script.innerHTML=`<div :class="[
        'bbn-textbox',
        componentClass,
        {'bbn-state-disabled': !!disabled}
      ]">
  <textarea :class="[
              'bbn-no-border',
              'bbn-radius',
              'bbn-w-100',
              'bbn-spadded',
              {'bbn-state-disabled': !!disabled}
            ]"
            style="max-width: 100%; min-width: 100%; min-height: 100%"
            :value="value"
            :name="name"
            ref="element"
            @input="emitInput($event.target.value)"
            @click="click($event)"
            @focus="focus($event)"
            @blur="blur($event)"
            @change="change($event)"
            @keydown="keydown($event)"
            @keydown.enter.stop=""
            @keyup="keyup($event)"
            @paste="$emit('paste', $event)"
            :disabled="disabled"
            :required="required"
            :placeholder="placeholder"
            :maxlength="maxlength"
            :rows="rows"
            :cols="cols"
  ></textarea>
</div>
`;script.setAttribute('id','bbn-tpl-component-textarea');script.setAttribute('type','text/x-template');document.body.insertAdjacentElement('beforeend',script);(function(bbn){"use strict";Vue.component('bbn-textarea',{mixins:[bbn.vue.basicComponent,bbn.vue.inputComponent,bbn.vue.eventsComponent],props:{rows:{type:Number},cols:{type:Number},maxlength:{type:Number},cfg:{type:Object,default:function(){return{}}}},methods:{clear:function(){this.emitInput('');}},mounted(){this.ready=true;}});})(bbn);})(bbn);